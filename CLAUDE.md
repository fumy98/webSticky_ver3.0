# WebSticky — CLAUDE.md

## プロジェクト概要

Webページに付箋を貼り付け、重要箇所へ素早く戻れるChrome拡張機能。

**解決する課題**
- 生成AIとの会話で、気になった回答が後続のやり取りで埋もれてしまう
- 縦長LPなど、後でじっくり見返したい箇所がある

## 要件

### 機能要件

- ページのY軸位置に付箋を貼る（スクロール量で位置を保持）
- 1ページあたり最大5枚まで付箋を貼れる
- 付箋を個別に削除できる
- 現在地（スクロール位置）も記憶する

### UI

- 画面右下に桜の花びら型の付箋アイコンを常時表示
- アイコンクリック → 常にリストを表示（1枚でもリスト表示）→ 選択した付箋位置へスクロール
- 付箋の追加は `Alt+S`（Mac: `Option+S`）で行う

### 付箋のライフサイクル

| 操作 | 付箋の状態 |
|------|-----------|
| リロード | 残る |
| ブラウザバック | 消える |
| 別ページへ遷移 | 消える |

## 技術スタック

- TypeScript
- Chrome Extensions API (Manifest V3)
- ビルドツール：Vite + vite-plugin-web-extension
- スタイル：素のCSS（CSS Modules）

## コーディング規約

- コメント・ドキュメント：日本語
- 命名規則：camelCase（変数・関数）、PascalCase（クラス・型）
- ファイル名：camelCase
- `any` 型：原則禁止
- コミットメッセージ：Conventional Commits（英語）
  - 例：`feat: add sticky note shortcut`, `fix: clear notes on navigation`

## 開発方針（Claudeへの指示）

- AI駆動開発：フラットなパートナーとしてディスカッションしながら進める
- **どんどん実装する**：設計の承認待ちはしない
- **判断に迷ったらベストな方を選んで実装し、後から報告する**
- シンプルさを優先し、過剰な抽象化はしない

## コマンド

```bash
# 開発
npm run dev

# ビルド
npm run build
```
